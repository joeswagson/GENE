namespace GENE.Flow.Typescript.Members.Number;

// js doesn't have as verbose of number typing as C#, so we need to handle decimal types as the user needs
public interface NumberType<T> : ITypeDefinition<T>;